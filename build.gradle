task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}

buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.0'
    }
}
